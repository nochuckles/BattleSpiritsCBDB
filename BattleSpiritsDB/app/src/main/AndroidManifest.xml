<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.battlespiritsdb">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/appicon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/appiconround"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".MainActivity"></activity>
        <activity
            android:name=".CardFilter"
            android:label="@string/title_activity_card_filter"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".CardsLeftActivity" />
        <activity
            android:name=".CardShowData"
            android:theme="@style/AppTheme.CustomTheme" />
        <activity android:name=".StockActivity" />
        <activity
            android:name=".CardListActivity"
            android:launchMode="singleTop" />
        <activity android:name=".OnStartAnimation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>